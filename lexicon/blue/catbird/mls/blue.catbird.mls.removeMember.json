{
  "lexicon": 1,
  "id": "blue.catbird.mls.removeMember",
  "description": "Remove (kick) a member from conversation (admin-only)",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Remove a member from the conversation. Admin-only operation. Server authorizes, soft-deletes membership (sets left_at), stores the MLS commit for distribution to remaining members, and logs the action.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["convoId", "targetDid", "idempotencyKey"],
          "properties": {
            "convoId": { 
              "type": "string",
              "description": "Conversation identifier"
            },
            "targetDid": { 
              "type": "string", 
              "format": "did",
              "description": "DID of member to remove"
            },
            "idempotencyKey": {
              "type": "string",
              "description": "Client-generated ULID for idempotent removal operations",
              "maxLength": 26
            },
            "commit": {
              "type": "string",
              "description": "Base64-encoded MLS commit message for the removal. Required for epoch advancement and distribution to remaining members."
            },
            "reason": {
              "type": "string",
              "maxLength": 500,
              "description": "Optional reason for removal (logged in audit trail)"
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["ok", "newEpoch"],
          "properties": {
            "ok": { 
              "type": "boolean",
              "description": "Whether removal succeeded"
            },
            "newEpoch": {
              "type": "integer",
              "minimum": 0,
              "description": "New epoch after the removal commit was applied"
            }
          }
        }
      },
      "errors": [
        { "name": "NotAdmin", "description": "Caller is not an admin" },
        { "name": "NotMember", "description": "Target is not a member" },
        { "name": "CannotRemoveSelf", "description": "Use leaveConvo to remove yourself" },
        { "name": "ConvoNotFound", "description": "Conversation not found" }
      ]
    }
  }
}
