{
  "lexicon": 1,
  "id": "blue.catbird.mlsChat.policy",
  "defs": {
    "main": {
      "type": "record",
      "key": "literal:self",
      "description": "Chat policy preferences for MLS messaging. Single mutable record per user.",
      "record": {
        "type": "object",
        "required": ["createdAt"],
        "properties": {
          "whoCanMessageMe": {
            "type": "string",
            "description": "Who can initiate conversations: 'everyone', 'mutuals', 'following', 'nobody'"
          },
          "allowFollowersBypass": {
            "type": "boolean",
            "description": "Allow followers to message even if whoCanMessageMe would exclude them"
          },
          "allowFollowingBypass": {
            "type": "boolean",
            "description": "Allow users you follow to message even if whoCanMessageMe would exclude them"
          },
          "autoExpireDays": {
            "type": "integer",
            "minimum": 1,
            "maximum": 30,
            "description": "Auto-expire conversations after N days of inactivity"
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
