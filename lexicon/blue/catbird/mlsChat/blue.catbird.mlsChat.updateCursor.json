{
  "lexicon": 1,
  "id": "blue.catbird.mlsChat.updateCursor",
  "description": "Update the read cursor position for a conversation",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Update the read cursor for a conversation to track the last-read position.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["convoId", "cursor"],
          "properties": {
            "convoId": {
              "type": "string",
              "description": "Conversation identifier"
            },
            "cursor": {
              "type": "string",
              "description": "Opaque cursor value representing the read position (e.g., seq number as string)"
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["updatedAt"],
          "properties": {
            "updatedAt": {
              "type": "string",
              "format": "datetime",
              "description": "Timestamp when the cursor was updated"
            }
          }
        }
      },
      "errors": [
        {"name": "ConvoNotFound", "description": "Conversation not found"},
        {"name": "NotMember", "description": "Caller is not a member of the conversation"},
        {"name": "InvalidCursor", "description": "The provided cursor value is invalid"}
      ]
    }
  }
}
