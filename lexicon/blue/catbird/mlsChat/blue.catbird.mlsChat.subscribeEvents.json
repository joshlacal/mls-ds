{
  "lexicon": 1,
  "id": "blue.catbird.mlsChat.subscribeEvents",
  "description": "Subscribe to live conversation events via WebSocket (consolidates subscribeConvoEvents with streamlined event types)",
  "defs": {
    "main": {
      "type": "subscription",
      "description": "Subscribe to live events (messages, membership changes, epoch advances, conversation updates) via firehose-style DAG-CBOR framing. Requires a valid ticket from getSubscriptionTicket.",
      "parameters": {
        "type": "params",
        "properties": {
          "ticket": {
            "type": "string",
            "description": "JWT ticket from getSubscriptionTicket for authentication"
          },
          "cursor": {
            "type": "string",
            "description": "Opaque resume cursor. If provided, resume after this position."
          }
        }
      },
      "message": {
        "schema": {
          "type": "union",
          "refs": [
            "#messageEvent",
            "#reactionEvent",
            "#typingEvent",
            "#newDeviceEvent",
            "#infoEvent",
            "#treeChanged",
            "#groupResetEvent",
            "#membershipChangeEvent",
            "#groupInfoRefreshRequestedEvent",
            "#readditionRequestedEvent",
            "#circuitBreakerTrippedEvent",
            "#resetRequestedEvent",
            "#welcomeReissueRequestedEvent"
          ]
        }
      }
    },
    "messageEvent": {
      "type": "object",
      "description": "A new encrypted message was sent",
      "required": ["cursor", "message"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor for this event position" },
        "message": { "type": "ref", "ref": "blue.catbird.mlsChat.defs#messageView" },
        "ephemeral": {
          "type": "boolean",
          "description": "When true, this is an ephemeral signal (typing, read receipt, presence) that should NOT be shown in chat history. Omitted (defaults to false) for regular persistent messages."
        },
        "epoch": {
          "type": "integer",
          "minimum": 0,
          "description": "MLS epoch this message was encrypted at. Recipient SHOULD reject events where epoch < localJoinEpoch BEFORE feeding to unprotect_message."
        }
      }
    },
    "reactionEvent": {
      "type": "object",
      "description": "A reaction was added or removed",
      "required": ["cursor", "convoId", "messageId", "did", "reaction", "action"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "messageId": { "type": "string", "description": "Message that was reacted to" },
        "did": { "type": "string", "format": "did", "description": "DID of the reactor" },
        "reaction": { "type": "string", "description": "Emoji or short code" },
        "action": { "type": "string", "knownValues": ["add", "remove"] }
      }
    },
    "typingEvent": {
      "type": "object",
      "description": "A user started or stopped typing",
      "required": ["cursor", "convoId", "did", "isTyping"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "did": { "type": "string", "format": "did", "description": "DID of the typist" },
        "isTyping": { "type": "boolean", "description": "True if started typing, false if stopped" }
      }
    },
    "newDeviceEvent": {
      "type": "object",
      "description": "A user registered a new device needing addition to the conversation",
      "required": ["cursor", "convoId", "userDid", "deviceId", "deviceCredentialDid", "pendingAdditionId"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "userDid": { "type": "string", "format": "did", "description": "Base user DID" },
        "deviceId": { "type": "string", "description": "Device identifier" },
        "deviceName": { "type": "string", "description": "Human-readable device name" },
        "deviceCredentialDid": { "type": "string", "description": "Full device credential DID (did:plc:user#device-uuid)" },
        "pendingAdditionId": { "type": "string", "description": "ID of the pending addition for claim/complete flow" }
      }
    },
    "treeChanged": {
      "type": "object",
      "description": "The canonical MLS tree state changed. Clients must compare confirmationTag against their local state and re-join if mismatched.",
      "required": ["cursor", "convoId", "confirmationTag", "epoch"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "confirmationTag": { "type": "bytes", "description": "Confirmation tag of the new canonical tree" },
        "epoch": { "type": "integer", "description": "New epoch number" }
      }
    },
    "infoEvent": {
      "type": "object",
      "description": "Informational message (heartbeat, notices, or GroupInfo refresh request)",
      "required": ["cursor", "info"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "info": { "type": "string", "description": "Human-readable info or keep-alive message" },
        "infoType": {
          "type": "string",
          "description": "Structured info type for programmatic handling",
          "knownValues": ["heartbeat", "groupInfoRefreshRequested", "readditionRequested"]
        },
        "convoId": { "type": "string", "description": "Conversation ID (for groupInfoRefreshRequested, readditionRequested)" },
        "requestedBy": { "type": "string", "format": "did", "description": "DID of the requester" }
      }
    },
    "groupResetEvent": {
      "type": "object",
      "description": "The MLS group was reset and a new group was created for this conversation",
      "required": ["cursor", "convoId", "newGroupId", "resetGeneration"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "newGroupId": { "type": "string", "description": "New MLS group ID after reset" },
        "resetGeneration": { "type": "integer", "description": "Cumulative reset count for this conversation" },
        "resetBy": { "type": "string", "format": "did", "description": "DID of the admin who initiated the reset" },
        "cipherSuite": {
          "type": "string",
          "description": "MLS cipher suite used for the new group after reset.",
          "knownValues": [
            "MLS_256_XWING_CHACHA20POLY1305_SHA256_Ed25519"
          ]
        },
        "reason": { "type": "string", "description": "Optional reason for the reset" }
      }
    },
    "membershipChangeEvent": {
      "type": "object",
      "description": "A membership change occurred (joined, left, removed, kicked)",
      "required": ["cursor", "convoId", "did", "action"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "did": { "type": "string", "format": "did", "description": "DID of the member involved" },
        "action": {
          "type": "string",
          "description": "Type of membership change",
          "knownValues": ["joined", "left", "removed", "kicked"]
        }
      }
    },
    "groupInfoRefreshRequestedEvent": {
      "type": "object",
      "description": "A GroupInfo refresh was requested for a conversation",
      "required": ["cursor", "convoId"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "requestedBy": { "type": "string", "format": "did", "description": "DID of the requester" },
        "requestedAt": { "type": "string", "format": "datetime", "description": "When the refresh was requested (RFC3339)" }
      }
    },
    "readditionRequestedEvent": {
      "type": "object",
      "description": "A member re-addition was requested for a conversation",
      "required": ["cursor", "convoId"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "requestedBy": { "type": "string", "format": "did", "description": "DID of the member requesting re-addition" },
        "requestedAt": { "type": "string", "format": "datetime", "description": "When the re-addition was requested (RFC3339)" }
      }
    },
    "circuitBreakerTrippedEvent": {
      "type": "object",
      "description": "The automatic group-reset circuit breaker tripped for this conversation. Auto-reset is disabled; an admin must intervene.",
      "required": ["cursor", "convoId", "resetCount", "trippedAt"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "resetCount": { "type": "integer", "description": "Recent reset count that triggered the trip" },
        "trippedAt": { "type": "string", "format": "datetime", "description": "When the breaker tripped (RFC3339)" }
      }
    },
    "resetRequestedEvent": {
      "type": "object",
      "description": "Phase 2.5: an indirect trigger (quorum vote, system sweep, inline 409/404) has requested a crypto session reset. Active members are invited to respond by submitting new MLS group material via bootstrapResetGroup or commitGroupChange. First commit wins via UNIQUE (conversation_id, generation) tie-break. The previous groupResetEvent is also emitted for backward compatibility during Stage 1.",
      "required": ["cursor", "convoId", "cryptoSessionId", "generation", "trigger", "requestEventId"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor for this event position" },
        "convoId": { "type": "string", "description": "Conversation identifier" },
        "cryptoSessionId": { "type": "string", "description": "Server-side opaque id of the crypto_session whose reset was requested (the prior session, now in state='reset_requested')" },
        "generation": { "type": "integer", "description": "Generation number of the prior session — clients should activate at generation+1" },
        "trigger": {
          "type": "string",
          "description": "Which subsystem produced the reset request",
          "knownValues": ["quorum_vote", "system_sweep", "inline_commit_409", "inline_groupinfo_404"]
        },
        "requestEventId": { "type": "string", "description": "Server-side opaque id of the crypto_session_reset_requested delivery_event for correlation" },
        "expectedNewMlsGroupId": { "type": "string", "description": "If non-null, only this mls_group_id may activate. Phase 2.5 indirect triggers always emit null here; admin/bootstrap direct flows always provide a value." },
        "reason": { "type": "string", "description": "Human-readable reason for the reset" },
        "requestedAt": { "type": "string", "format": "datetime", "description": "When the reset was requested (RFC3339)" }
      }
    },
    "welcomeReissueRequestedEvent": {
      "type": "object",
      "description": "A recipient device called reissueWelcome because it cannot decrypt the original Welcome. Server pushes this event to the inviter, who is expected to re-stage a new commit with a fresh KP. Body fields mirror blue.catbird.mlsChat.defs#welcomeReissueRequest.",
      "required": ["cursor", "convoId", "recipientDeviceDid", "requestedAt", "requestId"],
      "properties": {
        "cursor": { "type": "string", "description": "Resume cursor for this event position" },
        "convoId": { "type": "string", "description": "Conversation needing a reissued Welcome." },
        "recipientDeviceDid": { "type": "string", "description": "Recipient device that cannot decrypt the original Welcome. This may be a device-qualified DID (did#deviceId)." },
        "requestedAt": { "type": "string", "format": "datetime", "description": "When the reissue was requested (RFC3339)." },
        "requestId": { "type": "string", "description": "Server-generated request identifier. Inviter echoes this in the replacement commit's idempotencyKey." }
      }
    }
  }
}
